services:
  twitch-overlay:
    build: .
    container_name: twitch-multitask-overlay
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development